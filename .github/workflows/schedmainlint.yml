---
on:
  schedule:
    - cron: "0 5 * * */3"
name: Ansible Lint Upstream - Scheduled
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Ansible Lint Upstream - Scheduled
        uses: ./action-lint
